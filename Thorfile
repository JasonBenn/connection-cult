require 'pg'

class DB < Thor

  desc "reset", "drop db, create db, create tables"
  def reset
    # conn = PG.connect('postgres://jasonbenn@localhost/spirals')

    # reset_table = <<-SQL
    # DROP TABLE IF EXISTS spirals;
    # CREATE TABLE spirals (
    #   modified_at timestamp with time zone DEFAULT now() NOT NULL,
    #   id SERIAL PRIMARY KEY,
    #   email text,
    #   thickness bigint,
    #   grid_size bigint,
    #   color text,
    #   image_url text
    # );

    # COMMENT ON TABLE spirals IS 'For repo 7-20-15 (created 7-25-15)';
    # SQL

    # conn.exec(reset_table)
  end

end
